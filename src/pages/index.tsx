import { SignInButton, SignOutButton, useUser } from "@clerk/nextjs";
import { type NextPage } from "next";
import Head from "next/head";
import { StudentAvailabilityForm } from "~/components/studentAvailability/StudentAvailabilityForm";
import { StudentAvailabilityTable } from "~/components/studentAvailability/StudentAvailabilityTable";
import { TutorAvailabilityForm } from "~/components/tutorAvailability/TutorAvailabilityForm";
import { TutorAvailabilityTable } from "~/components/tutorAvailability/TutorAvailabilityTable";
import { LessonSchedule } from "~/components/lesson/LessonSchedule";

const Home: NextPage = () => {
  const user = useUser();

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center justify-center bg-slate-200">
        <nav className="flex w-full justify-end p-4">
          <div>
            {!!user.isSignedIn && (
              <div className="rounded bg-purple-600 p-4 text-white">
                <SignOutButton />
              </div>
            )}
          </div>
        </nav>
        {!user.isSignedIn && (
          <div className="flex min-h-screen flex-col items-center justify-center bg-gradient-to-br">
            <h1 className="mb-10 text-4xl font-bold text-slate-700">
              Welcome to Tutor Time
            </h1>
            <p className="mb-20 max-w-lg text-center text-lg text-slate-500">
              A scheduling app for tutors and students to help find shared
              availability
            </p>
            <div
              className="rounded-lg bg-purple-600 p-4 px-8 py-4 text-lg font-medium text-white
            shadow-lg hover:bg-purple-700"
            >
              <SignInButton />
            </div>
          </div>
        )}
        {!!user.isSignedIn && (
          <div className="w-full max-w-4xl">
            <TutorAvailabilityForm />
            <TutorAvailabilityTable />
            <StudentAvailabilityForm />
            <StudentAvailabilityTable />
            <LessonSchedule />
          </div>
        )}
      </main>
    </>
  );
};

export default Home;
